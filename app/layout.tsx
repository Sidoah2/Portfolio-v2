import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Ahmed Bouacheri | Full Stack Developer & AI Specialist",
  description: "Official portfolio of Ahmed Bouacheri, specializing in Flutter, AI integration, and Web applications with Next.js.",
  keywords: ["Ahmed Bouacheri", "Full Stack Developer", "AI Specialist", "Portfolio", "Flutter Developer", "Next.js"],
  authors: [{ name: "Ahmed Bouacheri" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
